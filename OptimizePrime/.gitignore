# Nothing here is committed by accident. This folder is a SUBMISSION package,
# not a working checkout — it carries source, docs and cited evidence only.
.env
.env.*
!.env.example
data/*.csv
bin/
.devbox/
node_modules/
*.log
