# Decisions Log — append-only, newest at top

- **2026-08-02 · teamkit/UNSEEN_DATASET.md — teammate-facing wrapper around `.mridul` §4 for running a new/sealed slice on your own laptop. Canonical repoint = ONE `.env` line `CLICKHOUSE_DATABASE=rca_unseen` (or `RCOS_DB=` for a one-off); every tool resolves the db via `default_db()` at call time — no per-tool `--db`/`RCOS_TABLE`. Steps: load (`load_clickhouse.py --parquet …`, builds ad_events+dims+`events`) → `run_incident.py --rebuild-cube` → `emit_ui_bundle.py` → run console. Notes the loader-`DROP` demo hazard, cube-is-a-snapshot (no MV), and `RCOS_API=off` parachute. Offline rehearsal via `gen_e2e_dataset.py` → `rca_e2e`. Linked from teamkit/README. Docs-only. (Supersedes the first draft of this file, which used the older per-tool-flag method.)**

> The lightweight sync mechanism for a 12-hour sprint. One line per decision that changes a
> contract, a schema, a scope, or an interface. Cheaper than a meeting. If you changed something
> another terminal depends on and it's NOT here, they don't know about it.
>
> Format: `HH:MM · who · what changed · which file/contract · why`

<!-- Add new entries directly under this line -->

- **EVENT · C · UI now defaults to the DEPLOYED engine API (owner report: UI showed fixtures, not the API): RCOS_API is resolved at call time from env OR .env (was import-time env-only, so the deployed URL couldn't take effect) — .env now carries http://23.101.175.68:8077 per #54's runbook; local :8000 remains the code default. Duplicate investigation id from the deployed scan de-duped in the UI (first wins) + filed as #63. 5 deployed investigations render end to end.**

- **EVENT · Lead · RCA Engine API is LIVE & PUBLIC on `mercury` (stable) + UI wired to it · scripts/deploy_mercury.sh, ui/incidents.py (RCOS_API), teamkit/GAPS.md.** `api/server.py` runs as systemd `rca-api` (uvicorn `0.0.0.0:8077`, Restart=always, enabled on boot); reachable at **`http://23.101.175.68:8077`** (`/health`, `/scan`, `/investigations`, `/docs`) — verified externally (`/scan` 0.78s, 4 real investigations from live ClickHouse). Two firewall layers opened: Azure NSG inbound `rca-api-8077` (pri 330, TCP 8077, Any) **and** host `ufw allow 8077/tcp` (ufw was the silent blocker — NSG alone wasn't enough). UI dashboard (`streamlit run ui/app.py`) now fetches incidents from the API via `RCOS_API=http://23.101.175.68:8077`; `ui/incidents._normalize_api()` maps the engine shape, `source_label()` shows live provenance, 60s cache + `scan_bundle.json` file fallback so it can't break mid-demo. Metrics tab still queries ClickHouse Cloud directly. Redeploy on any API change = **`bash scripts/deploy_mercury.sh`** (rsync → restart; firewall rules persist). A cloudflared HTTPS quick-tunnel (`rca-tunnel`) remains as backup but its URL is non-stable; the `:8077` IP is the canonical URL.
- **EVENT · C · Real-time timeline fix (owner report: graphs not following the data's timestamps): all heavy UI caches are now KEYED BY THE DATASET CLOCK (max(event_time), rechecked every 10 s) — new data invalidates caches automatically; static data still caches. Sidebar ↻ Refresh button clears every layer + POSTs the engine API /refresh. Reproduced + fix proven live on a time-shifted scratch table (new day appeared on reload, no restart). Note: the engine API caches its scan until /refresh — if a live feed updates the data, re-run the scan via ↻ or POST /refresh.**

- **EVENT · C · UI WIRED TO THE LIVE RCA ENGINE API (#48): incidents()/bundle.load() now source engine `/scan` first (normalized to the frozen §8.1 shape; RCOS_API overrides, `off` disables) → scan-bundle file → statics. All 4 real investigations render end to end — tiles, per-anomaly diagnosis at FULL depth (engine decomposition + evidence w/ SQL+query_id), chat/LibreChat ground on engine output. Fixtures remain the offline fallback. narrate() dict unwrapped; localhost fetches bypass system proxies.**

- **EVENT · C · UI is now DATASET-AGNOSTIC (owner: 'these pages must show up for any new dataset'): one input swap runs the whole console on new data — RCOS_TABLE=<db>.<events table> (live metrics/charts/row-count/clock), RCOS_SCAN_BUNDLE=<scan json> (incidents tab + grid shading, windows now DERIVED from incidents, not hardcoded), RCOS_BUNDLE=<§8 json> (per-anomaly diagnosis depth). Storyboard time axis derives from data+incident spans (verified re-anchoring to a synthetic September set). Smoke-tested live against rca_e2e.ad_events (9,906,064 rows) — worker's unseen synthetic slice renders end to end.**

- **EVENT · C · RCA is now PER-ANOMALY (owner: 'the RCA should be at an anomaly level'): Diagnosis page gained an incident selector + ?incident= deep links; every incident renders its bundle at honest depth — INC-C full §8 (waterfall/gates/ledger), others summary §8.1 evidence with 'pending engine emission' placeholders (never faked). Storyboard case files link to their own diagnosis. Includes the #37 scan-bundle adapter (supersedes that PR).**

- **EVENT · C · Diagnosis page shipped (?page=diagnosis, nav SOON removed): the full §8 RCA read-out — What moved (KPIs + {{ev_N}}-resolved narrative) → LMDI waterfall + factor verdicts → culprit card (rate/mix, purity, counterfactual) → gate funnel 1,709→1 + uniformity table + 3-bucket hypotheses → 18-row evidence ledger (query ids). Data = bundle.load() (golden fixture; RCOS_BUNDLE swaps in engine output). Storyboard's matching case links 'Full RCA evidence →'.**
- **EVENT · C · T1.1 DRAFT (needs ENG ratification, not frozen): scan-level golden fixture contracts/fixtures/scan_bundle.json (§8.1 {scan_summary, investigations[4]}, measured numbers only, unmeasured fields omitted) + ui/incidents.py now consumes it (bundle-first, statics fallback, env RCOS_SCAN_BUNDLE override). Engine handoff = run_incident.py writes this file; UI needs zero further changes. Storyboard eyebrow shows scan stats (4 found · 13,672 checked · 6 ruled out).**

- **EVENT · C · Incidents tab shipped (owner picked Approach C 'Investigation Storyboard' in /office-hours): ?page=incidents route + real sidebar nav; SWIMLANE timeline (one lane per incident, own metric sparkline + own shaded window — overlap-proof, per owner feedback) w/ synced crosshair like the metrics grid; 4-step guided case file (Anomaly→Investigation→Verdict→Action, arrow keys); pin card now compact w/ 'Open investigation →' deep link; chat-dock 'rcos-chat-seed' handoff so Ask-AI buttons pre-seed the dock. Static ui/incidents.py only; swap point unchanged.**

- **EVENT · C · Incident hover + click-to-pin diagnosis on the metrics grid: hovering an incident window shows the incident card in the tooltip (id, delta, cause); clicking pins a full RCA panel (what happened / diagnosis / ruled out / do next). Content is STATIC by owner directive (ui/incidents.py = answer-key store, incidents() is the single swap point for the upcoming incident API — no ClickHouse fetch for this layer).**

- **EVENT · C · Chat dock rebuilt NATIVE (owner: iframe looked like a webview, not part of the dashboard): bottom-right dock is now a pure NR One chat (preset question chips, [ev_N] chips, sessionStorage persistence) calling the shim directly (CORS added, SHIM_URL env). LibreChat unchanged as the full-chat surface via the dock's ↗ and sidebar link — same rootcauseos-rca endpoint, one brain, two surfaces.**

- **EVENT · C · LibreChat self-hosted in-repo (integrations/librechat/: compose + Mongo + librechat.yaml with rootcauseos-rca endpoint); dashboard embeds it via bottom-right chat dock iframe + sidebar link (LIBRECHAT_URL). Replaces 'teammate deploys LibreChat'. Demo login created at the venue, creds in team channel — never in the repo.**

- **EVENT · C · UI RESET by owner: all console UI deleted (app.py, theme.py, chat.py, panels/, tests/, fixtures/) — fresh build incoming · KEPT: ui/nr_one.py (THE design system), ui/bundle.py, ui/data.py (live CH client), ui/llm.py (grounded LLM chain, fallback now self-contained), ui/playbook.py, integrations/openai_shim.py (LibreChat, still verified working). Everything deleted is recoverable at e9547c9.**

- **EVENT · C · LibreChat reinstated as 4th integration via shim · CLAUDE.md · owner override: teammate deploys LibreChat; integrations/openai_shim.py exposes the evidence-grounded RCA agent as OpenAI-compatible model `rootcauseos-rca` (port 8601); pill deep-links via LIBRECHAT_URL; all answers validated + persisted to rca.recommendations. Also: team NR One design system (design-system/nr_one.py) adopted as THE console theme (vendored at ui/nr_one.py until design-system/ lands on main).**
- **PRE-EVENT · Lead (Deepak) + InMobi mentor · Trim to "lean but not naive" · teamkit/ARCHITECTURE.md
  · Mentor (InMobi): RCA = slice-by-dimension + compare-to-baseline + name-segment; don't over-engineer;
  ONLY the given dataset — no external data, no table sprawl. Footprint = 4 raw tables + ONE cube (same
  data pre-aggregated, kept for Fast). CUT: external events/correlation + layered data model (Deepak's
  over-reach), Shapley/order-sensitivity (§4), verdict state machine → 3 outcomes. KEEP: same-weekday
  baseline, contribution ranking + purity/2-D (needed to name the right segment on INC-D vs the iPhone-14
  dilution artifact), volume/effect gates, evidence+ruled-out. Acceptance = the 4 pointers (Fast/
  Trustworthy/Localized/Honest). @C — §4/§5/§6 trims touch your work; please confirm before we delete.**

- **EVENT · C · DEMO.md rewritten for the 4-tab console · teamkit/DEMO.md · owner chose tabs (twice) over single-scroll; beat sheet now traverses Health/What happened/Proof/Ask incl. funnel diagram, playbook card, global validator banner, Ask tab; ₹→USD. Supersedes the 'no tabs' spec.**

- **PRE-EVENT · D (proposal, PR) · Multi-incident emit shape · CONTRACTS §8.1 · a slice has ≥4
  planted anomalies (INC-A..D) across metrics/windows; single-investigation bundle would report
  one → multi-miss on the heaviest criterion. Proposes an additive `{scan_summary, investigations:[...]}`
  wrapper around the existing per-investigation shape (unchanged). Owners (A/B) to confirm whether
  scan is the runner default or a `--scan` mode. Does not touch the fixture/UI contract.**

- **PRE-EVENT · C · Baseline changed: same-hour-last-week → STL-on-residuals + MAD · CONTRACTS §3
  · n≈4–5 gives 41% band error and 11% downward bias in σ̂ → over-flagging. STL gives n≈56.
  Adds `1.4826/b_n` scaling, MAD=0 fallback chain, contamination exclusion. A OWNS THIS CHANGE.**
- **PRE-EVENT · C · Added rate/mix split + purity rule · CONTRACTS §5 · rate-effect ranking moves
  the noise floor ±70% → ±0.7% (100× SNR). Purity separates the true 2-D cell from its parents
  by 5.7×, which contribution alone cannot do. A + B OWN THIS.**
- **PRE-EVENT · C · Added uniformity gate + verdict state machine · CONTRACTS §6.1, §6.5 ·
  NORMAL / GLOBAL_UNLOCALIZED / LOCALIZED_1D / LOCALIZED_2D / INCONCLUSIVE. Without the gate we
  report INC-D as "iOS 18.1 −12.7%", understating it 4×. Without GLOBAL_UNLOCALIZED we fabricate
  a culprit for INC-A. B OWNS THE STATE MACHINE.**
- **PRE-EVENT · C · LMDI "exact by construction" reframed · CONTRACTS §4 · the revenue identity
  telescopes, so 0% residual is a tautology and would hold on a wrong metric tree. Validate via
  LMDI-vs-Shapley agreement + order sensitivity instead (single-order waterfall measured at 160%
  order sensitivity). DO NOT put "0% residual proves our method" on a slide.**
- **PRE-EVENT · C · LibreChat dropped; ClickStack promoted · CLAUDE.md · LibreChat is 6–10
  containers and replaces our dashboard with a chat window — the "must not weaken the primary
  solution" trap. Rules need ONE integration; Langfuse + ClickStack = two. C's scope changes.**
- **PRE-EVENT · C · `langfuse==4.14.2` pinned · CLAUDE.md · current SDK is v4; all v2/v3 patterns
  are removed. Use `shutdown()` not `flush()`, and `set_trace_as_public()` for judge access.**

- **PRE-EVENT · C · CTR baseline corrected 0.83% → 1.088% · docs/DATA.md · the old figure was
  `clicks/requests` (the `is_click` column mean), not `clicks/impressions`. Anything baselined
  on 0.83% is 31% low.**
- **PRE-EVENT · C · All four seen anomalies re-scanned and localized · docs/DATA.md · previous
  labels ("JP / iPhone-14", "EU/LATAM fill dips") were dilution artifacts, and 2 of 4 incidents
  were missed. True causes: INC-A global-unlocalizable, INC-B category=finance,
  INC-C os_version=Android 15, INC-D os_version=iOS 18.1 × region=APAC.**
- **PRE-EVENT · C · Golden fixture added · contracts/fixtures/example_bundle.json · real
  measured numbers for INC-C, incl. 6 overlapping segments so the UI can build + test the
  dilution-artifact treatment. Supersedes the `device_model=iPhone 14` example in CONTRACTS §8,
  which named a dilution artifact (−5.91%) rather than the true cause (−50.70%).**
- **PRE-EVENT · C · Search space quantified · docs/DATA.md · 62 one-dim + 1,647 two-dim = 1,709
  candidates; × 8 metrics = 13,672 checks = 114 human-hours. This is our Problem-fit number.**

- 00:00 · (example) B · evidence objects now carry `rows` field · CONTRACTS §7 · UI wants row count in chips
- 00:00 · (example) A · cube grain is hourly not daily · CONTRACTS §2 · detection needs hour-of-day seasonality

- **2026-08-01 (Naman):** tests/depth-suite-v2 — blind depth evaluation of the engine (33 planted incidents across 6 generated DBs). Two UNDOCUMENTED structures found in the organizer data: EU format swap Jun 16-20 (ecpm, revenue-neutral) and tier_3 CTR degradation Jun 16-26. Additive engine v2 (tests/e2e/run_incident_v2.py) scores 33/33 vs main engine 23/33; cherry-pick list in tests/e2e/FINDINGS.md. Engine files untouched.
- **2026-08-01 (Naman):** fixes/engine-audit-hardening — 3-auditor pass (stats/logic/codex). run_incident.py: ONE line, baseline day count from data (old hardcoded 35 fabricated +191% deviations on non-35-day slices — judge-facing). v2: peel-loop head-drop fix + short-slice guards + global-series all metrics. PRECISION-FIRST: MINRELS 0.10 all metrics (never report wrong; sub-10% misses accepted). All datasets precision 1.00. v1 detection backports still proposed (FINDINGS §6).
- **2026-08-01 (Naman):** feat/llm-adjudicator — per judge feedback: recall mode (0.05 floor) + real-time local-LLM adjudication of the sub-10% band over computed evidence (snr, residuals, decomposition). Invariant: LLM only moves candidates between trays, never touches >=10%/GLOBAL, conf<0.7 -> needs_review, failure -> deterministic 10% gate. Blind: d1 6/6@1.00, d2 10/11@1.00, t2 3/3; ~5-13s warm on codex-qwen25-coder-7b, offline-safe.
- **2026-08-01 (Naman):** ui/adjudication-trays — Incidents tab renders the adjudication bifurcation natively: scoreboard (Confirmed / Watchlist / Ruled-out) + three defined sections; storyboard iframe height scales with tile count (840px fixed clipped everything past ~4 tiles); cards use INC n titles (long engine ids forced tile min-width past the container = the clipped-grid bug, and collided for same-metric same-day incidents). Demo bundle tests/e2e/results/ui_bundle_LIVE.json = real-pipeline (rca --recall + adjudicate) with INC titles.
- **2026-08-02 (Naman):** feat/api-adjudication — /scan now runs engine v2 in RECALL mode (RCOS_ENGINE=v1 restores legacy) + LLM adjudication into trays (RCOS_ADJUDICATE=off disables; no backend => deterministic fallback, scan never blocks). ui/incidents.py folds API needs_review/suppressed trays in as marker-titled INC-n cards. adjudicate.py default model = codex-qwen25-coder-7b (hermes3-8b MISRULES the known-FP case — reproduced live through the API). NOTE for deploy: api/ cross-owner edit, announced; VM redeploy picks this up; without Ollama/OPENAI key on the VM the trays degrade deterministically.
- **2026-08-02 (Naman):** integrations/clickstack — the 4th leg is wired, and it is verified, not aspirational. Design choice: **one client proxy, not auto-instrumentation.** `integrations/otel.py` (tracer `rca.clickhouse`, service `rca-engine`) plus a `TracedClient` returned by `run_incident.py connect()` instrument every engine query site across run_incident.py, tests/e2e/run_incident_v2.py and api/server.py with **ZERO call-site edits**, because all three receive `cx` from the one `connect()`. `opentelemetry-instrument` was rejected: it adds a wrapper process and breaks under `uvicorn --reload/--workers`. No-op unless `CLICKSTACK_ENABLED=1`; a pre-flight TCP check means an unreachable collector prints one line and stays a no-op; `atexit` shutdown flushes spans on short CLI runs. Evidence objects now carry real `rows_read`/`bytes_read`/`duration_ms` from `X-ClickHouse-Summary` — the ledger UI (ui/diagnosis.py:378-384) already rendered these columns and showed 0, so **no UI code changed**. api/server.py gained stage spans and `wall_clock_s` is now MEASURED (it was hardcoded 0.0). Measured: detector equality gate **PASS** (bundles ON == OFF after stripping per-run query_id/duration_ms/wall_clock_s, 6 investigations each way); 30/30 evidence objects non-zero (e.g. 3,599,416 rows · 102.6 ms); overhead not measurable (OFF 25.6s/ON 25.2s, OFF 25.1s/ON 24.2s — the scan is round-trip bound, 192 query spans at ~154 ms avg); 198 spans per scan land in ClickStack's own ClickHouse; **GUARDRAIL PASS — no `otel_*` or `hyperdx_*` table in the graded service.** tests/test_otel.py = 4 checks, all green. TWO NON-OBVIOUS CONSEQUENCES, both cost real time: (1) **HyperDX UI is on host :8081, not :8080** — Tailscale holds 8080 on this machine and the failure is silent (docker reports the port published, lima logs `failed to set up forwarding tcp port 8080`, the UI is just unreachable); compose maps `8081:8080`, `CLICKSTACK_URL` defaults to http://localhost:8081, OTLP 4317/4318 unaffected. (2) **`trace.set_tracer_provider()` made Langfuse v4 leak into our collector** — Langfuse v4 is OTel-based too, so the global provider sent ITS reasoning spans to ClickStack; observed in `otel_traces`. Fix = a **PRIVATE** TracerProvider (`provider.get_tracer(...)`, never the global setter), which isolates both directions; `blocked_instrumentation_scopes=["rca.clickhouse"]` alone was NOT sufficient (it is still set). SCOPE LIMITS, stated: engine query path only — ui/data.py, scripts/load_clickhouse.py and scripts/gen_e2e_dataset.py use their own clients and are not instrumented; traces only, no logs/metrics pipeline; the reachability check runs once at init, so a collector that dies mid-run drops spans safely but will not re-attach without a restart. NOTE for deploy: deps are in requirements.txt **and** the hardcoded pip list in scripts/deploy_mercury.sh. Plan + results: `teamkit/PLAN_CLICKSTACK.md`.
- **2026-08-02 (Deepak):** ui/incident-trend-chart — each pin card now renders a **35-day segment sparkline straight from `rca.cube`** (flagged window in red, dashed baseline, and a muted "everyone else stayed normal" reference line for localized incidents; global incidents omit the reference). `ui/data.py::segment_series(metric, filters)` builds the series — dimension names **whitelisted** against the cube columns, values quoted, ratios sum/sum IN SQL. Culprit `cause` string is parsed to the filter (handles 1-D and 2-D `dim=val × dim=val`); SVG is rendered in **Python** (keeps the JS f-string free of brace-escaping) and cached per `(metric, filter)`; enrichment is **best-effort** — a failed ClickHouse fetch drops that one chart, never the card. Verified vs ground truth (INC-5 0.433 in-window / 0.785 rest, INC-4 126,052, INC-6 2-D 0.388 / 0.785); 6/6 cards render live. NOTE: this merged **bundled inside PR #100** (the sidebar-align PR) rather than as its own PR — hence this retro-note so the team knows the cards changed.
