# secrets
.env
.env.*
!.env.example

# data-of-record is ClickHouse, not the repo — never commit the raw dataset
data/
*.parquet
*.csv.gz

# python
__pycache__/
*.pyc
.venv/
venv/

# tooling / os
.playwright-mcp/
.DS_Store
.streamlit/secrets.toml
tests/last_battletest.json
tests/eval_runs/

# librechat deployment (secrets + runtime state)
integrations/librechat/.env
integrations/librechat/data-node/
integrations/librechat/images/

# demo/PR screenshots stay local — do not commit binaries to the shared repo
evidence/screenshots/
.squad/
.squad/
locks/
AGENTS.md
GEMINI.md
tests/last_detection_eval.json

# agent-tooling state and generated audit artifacts — machine-local, never shared
.gstack/
.codex-audit-bundle.json
