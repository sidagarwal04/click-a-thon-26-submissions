# Python deps for the RCA pipeline / tooling. langfuse is PINNED (v4 SDK — v2/v3 patterns removed).
clickhouse-connect>=0.7
langfuse==4.14.2
pyarrow>=15
pandas>=2
streamlit>=1.30
fastapi>=0.110
uvicorn>=0.29
openai>=1.40
# ClickStack (OTel -> HyperDX). Only imported when CLICKSTACK_ENABLED=1.
opentelemetry-sdk>=1.27
opentelemetry-exporter-otlp-proto-http>=1.27
