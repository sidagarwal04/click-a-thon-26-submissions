.DS_Store
.env
*.log

# Symlink to the organiser's dataset package (base_context.md, data/, specs/).
# Provided input, not source: it stays a link so the spec-6 drop lands in one
# place rather than in two trees that can disagree. Create it per-machine:
#   ln -s /path/to/Atlys inputs
inputs

.idea/
.vscode/

__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

requirements.lock
**/graphify-out/
**/group.com.docker.bak/**
