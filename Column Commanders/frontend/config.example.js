// Copy to config.js (gitignored) and fill in real values for local dev.
// config.js is loaded before the HyperDX init script in index.html.
window.RCA_CONFIG = {
  hyperdxApiKey: '',              // omit/empty for Managed ClickStack
  hyperdxUrl: 'http://localhost:4318',
};
