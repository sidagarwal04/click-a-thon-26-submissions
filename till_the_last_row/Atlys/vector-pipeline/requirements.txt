# parquet_reader.py runtime dependencies
# Install with: pip install -r requirements.txt

# ClickHouse Python client (Apache 2.0)
clickhouse-connect>=0.7.0

# urllib3 is a transitive dep of clickhouse-connect but pin for reproducibility
urllib3>=2.0.0

# Parquet reading (Apache 2.0)
pyarrow>=14.0.0
