# Local override: mount librechat.yaml so the RCA custom endpoint (JAL-87) is picked up.
# The base docker-compose.yml does not mount the config; this adds it without editing that file.
services:
  api:
    volumes:
      - ./librechat.yaml:/app/librechat.yaml
