-- O4: session-level vs user-level concurrency, at the peak minute
-- session-level (served) is uniqExact(video_session_id) grouped by dims and minute
-- user-level (not served) would be uniq(user_id), HyperLogLog, mergeable via
-- uniqState/uniqMerge and bounded in memory, per D16

peak minute 29758276
concurrent sessions      22,175
concurrent users, exact  21,299
concurrent users, approx 21,299 (0.00% error vs exact)

sessions with more than one user_id: 303
users with more than one concurrent video_session_id (same account, multiple devices): 16327

reading: 876 of the peak's 22,175 sessions are a
second device on an account already counted, so user-level concurrency is
4.1% lower than session-level at the peak.
uniq matches uniqExact exactly at this cardinality, so uniqState/uniqMerge is a safe, bounded choice if user-level concurrency is ever served.

checked: uniq equals uniqExact (True), and distinct users never exceed
distinct sessions at the peak (True).
