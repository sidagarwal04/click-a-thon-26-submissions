# Optional. The dashboard already reads ../.env (the same file scripts/ch.sh and
# the retired demo server use), so you only need this if you want frontend-specific overrides
# without touching the repo-root credentials.
#
# CH_HOST=xxxx.region.clickhouse.cloud
# CH_PORT=8443
# CH_USER=default
# CH_PASSWORD=
# CH_DATABASE=phoenix