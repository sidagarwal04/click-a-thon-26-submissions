export enum TimeGrain {
  DAILY = "DAILY",
  WEEKLY = "WEEKLY",
  MONTHLY = "MONTHLY",
}

export enum TimeGrainOffset {
  DAILY = 1,
  WEEKLY = 7,
  MONTHLY = 30,
}

export enum TimeGrainAPIKey {
  DAILY = "day",
  WEEKLY = "week",
  MONTHLY = "month",
}

export enum TimeGrainPrefix {
  DAILY = "D",
  WEEKLY = "W",
  MONTHLY = "M",
}
