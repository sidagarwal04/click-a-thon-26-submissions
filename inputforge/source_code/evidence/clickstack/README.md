# ClickStack trace evidence

`trace-search.png` — ClickStack (HyperDX) trace search for `ServiceName =
sentinel-web`, `SpanName` in (`HTTP GET`, `longtask`), time range
Aug 2 12:09:49–12:24:49, 1,004 results across the graded run window.
Instance: the ClickHouse database configured via `CLICKHOUSE_URL` in
`apps/detection-service/.env.local` (same instance detection and
investigation query — see root README's ClickStack section).
