# Atlys Copilot orchestration service (ENGINEERING.md §4.1)
fastapi>=0.110
uvicorn[standard]>=0.29
mcp>=1.10,<2          # MCP server (SSE) — pinned to the stable 1.x decorator API
clickhouse-connect>=0.8.3
langfuse>=2.30,<3    # tracing.py uses the v2 SDK API (start_trace/span); v4 removed it
pydantic>=2.0
python-multipart>=0.0.9   # form/file uploads for POST /api/specs
httpx>=0.27              # async streaming proxy for POST /api/proxy/chat → LibreChat
