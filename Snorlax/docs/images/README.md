# Screenshots

Images referenced by the top-level [`README.md`](../../README.md).

| File | What it shows | Status |
|---|---|---|
| `snorlax-icon.svg` | Snorlax icon shown next to the title in the main README | ✅ included (vector, no file needed) |
| `daily-wrapped.svg` | Daily Wrapped cover card (vector mock of the hero image) | ✅ included — replace with a real `.png` screenshot if preferred |
| `daily-wrapped-primetime.png` | Prime-time / hour-bars card _(optional)_ | optional |
| `dashboard-overview.png` | Main Streamlit dashboard _(optional)_ | optional |

To capture more: run the dashboard (`cd sonyliv-dashboard-py && streamlit run app.py`),
open the **Daily Wrapped** view, and screenshot each card at ~480px-wide window for a
consistent look. Drop the PNGs here and reference them from the main README.
