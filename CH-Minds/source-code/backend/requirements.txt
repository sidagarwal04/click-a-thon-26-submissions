fastapi==0.115.0
uvicorn[standard]==0.30.6
clickhouse-connect==0.8.5
pydantic==2.9.2
langfuse==2.53.9
openai==1.51.0
anthropic==0.36.0
google-generativeai==0.8.3
# openai==1.51.0 still passes a `proxies` kwarg that httpx>=0.28 removed;
# pip's resolver otherwise picks httpx 0.28.x to satisfy anthropic/
# google-generativeai, breaking the OpenAI client at runtime (confirmed
# against a real run: "Client.__init__() got an unexpected keyword
# argument 'proxies'"). Pin httpx to the last version all three SDKs were
# built against.
httpx==0.27.2
