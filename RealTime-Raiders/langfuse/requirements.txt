# Prompt-ops pipeline — runs standalone on your machine, NOT in a container.
# Targets the Langfuse v4 Python SDK.
langfuse>=4.0,<5
openai>=1.40
anthropic>=0.39
clickhouse-connect>=0.7
python-dotenv>=1.0
requests>=2.31